agent:
    init:
        batch_size: 10
        samples_per_insert: 2
        min_replay_size: 10
    evaluate:
        num_episodes: 100
    learn:
        num_episodes: 100
network:
    init:
        foo: 100
environment:
    init:
        bar: 200
__global__:
    backing_dir: "./.aos/data"
    discount: .99
